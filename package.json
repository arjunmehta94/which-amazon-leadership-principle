{
  "private": true,
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "next build && next export && git add out/ && git commit -m \"out Deploy gh-pages\" && git subtree push --prefix out origin gh-pages",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "gh-pages": "^3.2.3",
    "lodash": "^4.17.21",
    "next": "latest",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  }
}
